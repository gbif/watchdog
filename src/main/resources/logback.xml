<configuration debug="true">

    <property name="defaultPattern" value="%-5level [%date{'HH:mm:ss,SSSZ'}] %logger: %msg%n%xEx"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>DEBUG</level>
        </filter>
        <encoder>
            <pattern>${defaultPattern}</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <append>false</append>
        <file>watchdog.log</file>
        <encoder>
            <pattern>${defaultPattern}</pattern>
        </encoder>
    </appender>

    <logger name="org.gbif.OrphanDatasetScanner" level="DEBUG"/>
    <logger name="org.gbif.dwc" level="WARN"/>
    <logger name="org.gbif.common.parsers" level="ERROR"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>
